# Session — 2026-04-13

## Completed
- [x] Full backend folder structure created (webhook/, agent/, integrations/, auth/, db/, jobs/, middleware/)
- [x] `backend/app.py` — Flask app with /health, all API routes, edit detection scheduler wired
- [x] `backend/db/schema.sql` — full schema (pms, oauth_tokens, style_samples, draft_history, clients, sessions)
- [x] `backend/db/__init__.py` — Supabase singleton client
- [x] `backend/db/models.py` — all queries (get_pm_by_email, upsert_pm, get/upsert oauth tokens, get/add/delete style_samples, save_draft, get_draft_history, sessions, edit detection helpers)
- [x] `backend/auth/google_oauth.py` — token exchange endpoint, Fernet encrypt/decrypt, get_credentials_for_pm with auto-refresh
- [x] `backend/webhook/avoma.py` — HMAC verification, 200-immediate, background thread, full pipeline, rate limiter (10/min), Slack #eng-alerts on failure
- [x] `backend/agent/extractor.py` — Claude Call 1: transcript → structured JSON context
- [x] `backend/agent/prompt_builder.py` — style sample selection logic, system + user prompt construction
- [x] `backend/agent/generator.py` — Claude Call 2: email generation, preview_mode with SAMPLE_TRANSCRIPT
- [x] `backend/integrations/gmail.py` — create Gmail draft, get_sent_messages_since (for edit detection)
- [x] `backend/integrations/calendar.py` — attendee fallback lookup
- [x] `backend/integrations/slack.py` — DM PM with Block Kit message, lookup_by_email
- [x] `backend/jobs/edit_detection.py` — 30-min background job, sent message matching, diff computation, status updates
- [x] `backend/middleware/rate_limit.py` — 10 req/min per IP
- [x] `backend/Procfile` + `runtime.txt` — Railway deployment config
- [x] `.env.example` — all required keys documented
- [x] `backend/requirements.txt` — matches approved list exactly
- [x] `backend/test_extractor.py` — smoke test for extractor
- [x] `docs/avoma-webhook-payload.json` — realistic test payload
- [x] `frontend/` — Next.js 14 scaffolded (TypeScript, App Router, Tailwind)
- [x] `frontend/lib/supabase.ts` + `frontend/lib/api.ts` — all API helpers and types
- [x] `frontend/middleware.ts` — session cookie check on all /dashboard/* routes
- [x] `frontend/app/page.tsx` — landing page with Google sign-in button
- [x] `frontend/app/api/auth/google/route.ts` — Google OAuth redirect builder
- [x] `frontend/app/auth/callback/route.ts` — code exchange, sets httpOnly cookies, redirects
- [x] `frontend/app/dashboard/page.tsx` — redirect to onboarding or history
- [x] `frontend/app/dashboard/onboarding/page.tsx` — 3-step onboarding UI
- [x] `frontend/app/dashboard/training/page.tsx` — style sample management + live Claude preview
- [x] `frontend/app/dashboard/history/page.tsx` — draft history, stats, transcript/draft/diff expanders

## In progress
- [ ] None — all tasks through Task 08 are implemented

## Decisions made
- Edit detection uses threading not APScheduler (no new dep, start_background_scheduler called at app startup)
- Rate limiting is in-memory (sufficient for single-process Railway deployment; can move to Redis later)
- Diff stored as {removed: [], added: []} — simple enough for UI rendering without a library
- Frontend reads pm_id from httpOnly cookie server-side for dashboard redirect, client-side via document.cookie for client components
- `/api/pm/<pm_id>` endpoint added to Flask for dashboard onboarding-complete check

## Blockers
- Needs Google Cloud Console project + OAuth credentials to test OAuth flow
- Needs Supabase project + schema.sql run to test DB queries
- Needs real ANTHROPIC_API_KEY to test agent pipeline

## Next session starts at
Manual testing: set up .env, run schema.sql in Supabase, test Flask /health, test extractor, simulate webhook with docs/avoma-webhook-payload.json
